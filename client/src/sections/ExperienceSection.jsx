import React from 'react'

const ExperienceSection = ({sectionId, ref}) => {
  return (
    <section ref={ref} id={sectionId} className="section">ExperienceSection</section>
  )
}

export default ExperienceSection